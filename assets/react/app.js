import React, { Component, PropTypes} from 'react';
import ReactDOM from 'react-dom';
import MainComponent from './main'
ReactDOM.render( <MainComponent data={employee_data}/>, document.getElementById('application'))